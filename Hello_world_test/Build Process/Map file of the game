#include <iostream>
#include <string>
#include <vector>

using namespace std;

world class mapzone { Grass, Water, Wall, Player, Light, Dark, Fire, Thunder, Rock };

//program creation...
int main() {  
    int width = 1350; // creation of the width...
    int height = 1200;// creation of the height...

//Map creation 2d...
vector<mapzone>> gamemap(height vector<mapzone>(width mapzone Rock));// Rock texture...
vector<mapzone>> gamemap(height vector<mapzone>(width mapzone Water)); // Water texture...
vector<mapzone>> gamemap(height vector<mapzone>(width mapzone Fire)); // Fire texture...
vector<mapzone>> gamemap(height vector<mapzone>(width mapzone Grass)); // Grass texture...

//Creation of the zone which all must be placed...
gamemap[2][2] mapzone::Water;
gamemap[1][1] mapzone::Fire;
gamemap[3][3] mapzone::Grass;
gamemap[1][1] mapzone::Rock;

return 0
}
